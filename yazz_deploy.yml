apiVersion: template.openshift.io/v1
kind: Template
message: Installing Yazz
metadata:
  annotations:
    description: Yazz Installer 3
    iconClass: icon-3scale
    openshift.io/display-name: Yazz Installer 3
    openshift.io/provider-display-name: Yazz
    tags: integration, api management, javascript
  creationTimestamp: null
  name: yazz-installer-3




objects:
  - apiVersion: "v1"
    kind: "DeploymentConfig"
    metadata:
      name: "yazz"
    spec:
      template:
        metadata:
          labels:
            name: "yazz"
        spec:
          containers:
          - name: "yazz"
            image: "zubairq/pilot:latest"
            ports:
            - containerPort: 3000
              protocol: "TCP"
            readinessProbe:
                failureThreshold: 3
                httpGet:
                  path: /readiness-check
                  port: 3000
                  scheme: HTTP
                periodSeconds: 10
                successThreshold: 1
                timeoutSeconds: 1
            livenessProbe:
                        failureThreshold: 3
                        httpGet:
                          path: /live-check
                          port: 3000
                          scheme: HTTP
                        periodSeconds: 10
                        successThreshold: 1
                        timeoutSeconds: 1
      replicas: 1
      selector:
        name: "yazz"



  - apiVersion: v1
    kind: Service
    metadata:
      name: yazz-service
    spec:
      ports:
        - port: 3000
          protocol: TCP
          targetPort: 3000
      selector:
        deploymentconfig: yazz



  - apiVersion: v1
    kind: Route
    metadata:
        name: yazz-route
    spec:
      to:
        kind: Service
        name: yazz-service
    labels:
      app: yazz
